import { StarCursor } from './star-cursor';

describe('StarCursor', () => {
  it('should create an instance', () => {
    const directive = new StarCursor();
    expect(directive).toBeTruthy();
  });
});
